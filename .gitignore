# coding environments
.idea
.vs
.vscode
.fleet

# MacOS
.DS_STORE

# compiled bundler binary (use `go run ./tools/bundle`)
/bundle

# generated single-file bundles (build on demand via `make bundle`)
/dist/
